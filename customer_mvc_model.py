class CustomerModel:
    def do_I( self.customers, customer ):
        customers.append(customer)
        index = len(customers) - 1
        return index

    def do_U( self.customers, customer ):
        print(customer)
        customers[index] = customer
        return index
# pickle.dump(customers, write_customer_list)
# print(customer)
# print('전체 고객: ', customers)

